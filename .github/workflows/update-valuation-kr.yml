name: Update KR Valuation + Freeze (PYKRX)

on:
  workflow_dispatch: {}
  schedule:
    # 16:00 KST = 07:00 UTC (매일)
    - cron: "0 7 * * *"

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run KR valuation updater (PYKRX)
        run: |
          python scripts/update_valuation_kr.py

      - name: Inject metrics into Freeze + update theme index
        run: |
          python scripts/build_freeze.py

      - name: Commit & push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          if git diff --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git add data/cache/valuation_kr.json
          git add data/theme/T_*.json
          git add data/theme/index.json

          git commit -m "chore: KR valuation + freeze update (16:00 KST, PYKRX)"
          git push