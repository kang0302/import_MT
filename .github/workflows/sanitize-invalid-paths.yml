name: Sanitize invalid Windows paths

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sanitize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run sanitizer
        run: python scripts/sanitize_invalid_paths.py

